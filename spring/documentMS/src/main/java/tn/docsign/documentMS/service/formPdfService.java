package tn.docsign.documentMS.service;
/*
import org.apache.pdfbox.pdmodel.PDDocument;
import org.apache.pdfbox.pdmodel.PDPage;
import org.springframework.stereotype.Service;
import tn.docsign.documentMS.dto.MailDTO;

import java.io.File;
import java.io.IOException;
import java.util.List;
import java.util.stream.Collectors;
@Service*/
public class formPdfService {
}
/*



        private void processPages() throws IOException

        {   PDDocument sourceDocument= PDDocument.load(new File("src/main/resources/wwii.pdf"));
            for (int i = 0; i < sourceDocument.getNumberOfPages(); i++)
            {
                PDPage page = sourceDocument.getPage(i);
                if (currentPageNumber + 1 >= startPage && currentPageNumber + 1 <= endPage)
                {
                    processPage(page);
                    currentPageNumber++;
                }
                else
                {
                    if (currentPageNumber > endPage)
                    {
                        break;
                    }
                    else
                    {
                        currentPageNumber++;
                    }
                }
            }
        }


    public void createTextField(){

    }
}*/
